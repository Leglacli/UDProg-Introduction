#include "my.h"
#include <iostream>

using namespace std;

int foo = 7;

/* Commenting out main because last part of drill

int main()
{
	// Printing foo
	print_foo();
	
	// Printing 99
	print(99);

	int a = 7;
	int b = 9;

	cout << "First number before swap: " << a << ", second number before swap: " << b << endl;
	swap_v(a,b);
	cout << "First number after swap: " << a << ", second number after swap: " << b << endl;

	cout << "First number before swap: " << a << ", second number before swap: " << b << endl;
	swap_r(a, b);
	cout << "First number after swap: " << a << ", second number after swap: " << b << endl;

	const int ca = 7;
	const int cb = 9;

	cout << "First number before swap: " << ca << ", second number before swap: " << cb << endl;
	swap_v(ca, cb);
	cout << "First number after swap: " << ca << ", second number after swap: " << cb << endl;

	//swap_r(ca, cb); Can't change const
	
	double da = 7.7;
	double db = 9.9;

	cout << "First number before swap: " << da << ", second number before swap: " << db << endl;
	swap_v (da, db);
	cout << "First number after swap: " << da << ", second number after swap: " << db << endl;

	//swap_r (7.7, 9.9);

	return 0;
}

*/